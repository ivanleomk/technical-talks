---
id: interactions-api
title: Interactions API tour
tags: [api, tools, background, state]
duration_min: 8–12
---

# Interactions API tour

**Point:** complex apps stay small — state, background, typed steps.

## Beats
1. Hello interaction (`snippets/01`)
2. Sync function calling + `previous_interaction_id` (`02`)
3. `background=True` (`03`)
4. Background + tools / `requires_action` (`04`)

## Artifacts
- `snippets/01`–`04`
- `logs/trace.jsonl`, `SNIPPET_RESULTS.md`
